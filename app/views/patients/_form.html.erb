<%= form_with(model: @patient, local: true, class: "ui form") do |f|%>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Patient's Name"%>
  </div>
<%= f.submit nil, class: "ui purple button inverted"%>
<% end %>